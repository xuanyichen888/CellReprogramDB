# Curation / QA pipeline dependencies (NOT needed to run the Streamlit app).
# Install only when running the extraction/curation scripts:
#   pip install -r requirements-pipeline.txt
openai>=1.0.0          # DeepSeek API client (fill_*_deepseek.py, classify_paper_type_deepseek.py, extract_recipes.py)
requests>=2.28.0       # Europe PMC full-text fetching (fetch_fulltext*.py)
biopython>=1.81        # PubMed access (Bio.Entrez) in fetch_pubmed.py and enrich scripts
openpyxl>=3.1.0        # Excel read/write for QA workbooks
